Requirements:
Find (using hashtable(o map por falta de HT) for fast access. string(word), Object)

Object contains (Histogram)
-Total amount of repetitions
-Container(book list) to know textfiles and counter (something sortable.)
#--Array needs 1 master array. Easier to sort. A lot of unused memory.
#--Map easy to keep track, less memory. Harder to sort.
--Linked list are easy to sort, add & keep track. Low on memory. Winner.

DONE
Object Book contains
-Title (string)
-Count (int)

Sort it (O(n^2)) 
Intersection seria (O(n))

----------------

